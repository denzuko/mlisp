# Compiled binaries (all built via make build-all)
bin/mlisp
bin/mlisp-bugs
bin/mlisp-procmail-gen
bin/neural

# ASDF / SBCL compiled artefacts
*.fasl
*.abcl
*.fas
*.lib
*.x86f
*.sparcf
*.dfsl
*.lx32fsl
*.lx64fsl
src/mlisp.lisp.bak

# ASDF output cache
.cache/

# Quicklisp local
quicklisp/

# Runtime state (don't commit live subscriber data)
state/audit.sexp
metrics/
